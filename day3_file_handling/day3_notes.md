File handling is the process of creating a file, changing file contents, view contents in the file and deleting file using python language.

Backend systems store data to maintain user identity, enable functionality, ensure security, support business logic, and allow recovery and analysis.

File storage are non structural format of sharing and database is structural format of storing data, files are useful for small datasets where databases are good for large dataset, We can't search efficient in files but we search efficient in database.

Unauthorized access of data, so any one can change the contents.