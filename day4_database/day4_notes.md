SQLite is inbuild database for Python. It's a structural database use for store data, view data, and modify.

Database is more efficient than file system. It is more secure, authorization, easy to use than files

UNIQUE constraint is used to allow the user to enter only unique data and not allow duplicate data. It is used for mobile number, email and username for Instagram because they are unique for everyone.

Efficient search and modify